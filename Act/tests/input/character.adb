with Ada.Text_IO;
use Ada.Text_IO;

procedure character is
	x : Integer;
	y : Float;
	procedure main is
		i,j : Integer;
	begin
		i := 1 + i*i;
		j := -i*j + i - -2*(-(j*i));
		print_int (i);
	end;
begin
	main;
end;